{
  "rewrites": [
    {
      "source": "/account/:path(.*\.(js|css))",
      "destination": "/account/:path*"
    },
    {
      "source": "/:path((?!account/).*)",
      "destination": "https://test-next-navy.vercel.app/:path*"
    }
  ]
}
